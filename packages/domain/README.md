# packages/domain

複数 runtime で共有する domain vocabulary と軽量 model を置く。

対象:

- source family id
- Evidence state
- review state
- score method id
- domain enum

Python / TypeScript の二重実装が必要になったら、この配下で runtime 別に分ける。
